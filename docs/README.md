# Dokku Homebrew Tap

A homebrew tap for Dokku and related tools.

## Installation

```bash
brew tap dokku/repo
```

## Formulas

### dokku

The official remote client for the Dokku PaaS.

```bash
brew install dokku/repo/dokku
```

### docker-container-healthchecker

A health checker for Docker containers.

```bash
brew install dokku/repo/docker-container-healthchecker
```

## Updating

To update to the latest version:

```bash
brew update
brew upgrade dokku/repo/<formula>
```
